# GitHub Actions Workflow pour vérifier les notifications toutes les heures
# Alternative GRATUITE au cron Vercel (limité à 1x/jour sur plan Hobby)
#
# Pour activer :
# 1. Renommez ce fichier en "notifications.yml" (retirez .example)
# 2. Ajoutez le secret CRON_SECRET dans GitHub > Settings > Secrets and variables > Actions
# 3. Committez et poussez

name: Check Notifications

on:
  schedule:
    # Toutes les heures à la minute 0
    - cron: '0 * * * *'

  # Permet de déclencher manuellement depuis l'UI GitHub
  workflow_dispatch:

jobs:
  check-notifications:
    runs-on: ubuntu-latest

    steps:
      - name: Check Chess Notifications
        run: |
          curl -X GET "https://v-chess.vercel.app/api/cron/check-notifications" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            -w "\nHTTP Status: %{http_code}\n" \
            -s -o response.json

          echo "Response:"
          cat response.json

      - name: Upload response artifact
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: notification-check-response
          path: response.json
          retention-days: 7
